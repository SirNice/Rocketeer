const Discord = require("discord.js");
const fs = require('fs');

module.exports = {
    nombre: "help",
    alias: ['commands', 'ayuda'],
    description: "Description",
    run: async (client, message, args) => {



        const embedHelp = Discord.MessageEmbed()
        .setTitle('Help commands')



    }
} 